language: swift
osx_image: xcode8
before_install:
    - brew update
    - if brew outdated | grep -qx xctool; then brew upgrade xctool; fi
script: xctool -workspace Bold-WS.xcworkspace -scheme BoldTests test
